<h1>Swapi store view</h1>
<ul>
    <?php foreach($block->getSwapiData() as $item): ?>
        <li><?= (isset($item->title) ? $item->title : $item->name ?? $item)?></li>
    <?php endforeach; ?>
</ul>
